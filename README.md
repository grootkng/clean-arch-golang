# Clean Arch Golang

## How to run

### Requirements
* docker
* docker-compose

### Step by step
Open your terminal and type it:
```sh
docker compose up -d
```

And after that, the application should be running at `localhost:8080`